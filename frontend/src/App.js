// This project ships a complete, working vanilla JS dashboard in
// /frontend/public (index.html + app.js + styles.css) — that is what
// server.js serves and what's wired to every API route.
//
// This /src tree is left as a starting point if you later want to rebuild
// the dashboard as React (e.g. for embedding via Shopify App Bridge /
// Polaris inside the Shopify admin). It is NOT wired up yet.
import React from 'react';

export default function App() {
  return (
    <div>
      <h1>CRO Audit — React shell (not yet implemented)</h1>
      <p>See /frontend/public for the working dashboard.</p>
    </div>
  );
}
